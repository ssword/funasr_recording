[project]
name = "funasr-recording"
version = "0.1.0"
description = "Desktop recording and transcription app using FunASR via WebSocket"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "PySide6>=6.5.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0.0",
    "mypy>=1.0.0",
]
